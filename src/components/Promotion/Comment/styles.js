import styled from 'styled-components';

export const Container = styled.div`
  color: #95A5A7;
  font-weight: 700;
  font-size: 16px;
  white-space: nowrap;
  text-overflow: ellipsis;
  max-width: 445px;
  overflow: hidden;
`;
